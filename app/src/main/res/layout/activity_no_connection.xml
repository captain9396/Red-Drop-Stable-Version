<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1">

    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_weight="0.11"
        app:srcCompat="@drawable/no_network"
        android:layout_marginTop="53dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/noConnectionTextView"
        android:layout_width="330dp"
        android:layout_height="100dp"
        android:layout_below="@+id/imageView2"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="33dp"
        android:text="Looks like you are not connected to the internet. Please check your internet connection and try again."
        android:textSize="18sp" />

    <Button
        android:id="@+id/refreshButton"
        android:layout_width="330dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="11dp"
        android:background="@color/colorAccent"
        android:text="refresh"
        android:textColor="@android:color/background_light"
        android:layout_below="@+id/noConnectionTextView"
        android:layout_alignStart="@+id/noConnectionTextView" />

</RelativeLayout>